class ServerException implements Exception {}
// ServerException représente une erreur qui pourrait se produire lors de la communication avec le serveur (ex. échec d'une requête API).

class CacheException implements Exception {}
// CacheException représente une erreur liée à la gestion du cache (ex. échec de lecture ou d'écriture dans le cache).

//deux exceptions personnalisées en Dart implémentant l'interface Exception.

